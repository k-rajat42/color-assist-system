# Color Assist System — dependencies
# Install with:  pip install -r requirements.txt

opencv-contrib-python>=4.8.0   # includes legacy tracker (MOSSE) and CSRT/KCF
numpy>=1.24.0
pyttsx3>=2.90
pytest>=7.4.0
